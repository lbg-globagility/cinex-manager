<UserControl x:Class="Paradiso.TicketPatronControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             >
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
        <ComboBox Name="PatronComboBox" Margin="0, 0, 5, 0" Width="450" FontFamily="Arial" FontSize="14" ItemsSource="{Binding Path=Ticket.Patrons,Mode=TwoWay}" SelectionChanged="PatronComboBox_SelectionChanged"  SelectedValuePath="Key" SelectedValue="{Binding Ticket.PatronKey}"/>
        <xctk:IntegerUpDown Name="QtyIntegerUpDown" Width="60" Minimum="{Binding Path=Ticket.MinQuantity,Mode=TwoWay}" Maximum="{Binding Path=Ticket.MaxQuantity, Mode=TwoWay}" TextAlignment="Center" FontFamily="Arial" FontSize="14" ValueChanged="QtyIntegerUpDown_ValueChanged" Value="{Binding Path=Ticket.Quantity, Mode=TwoWay}" IsReadOnly="False" IsEnabled="False" />
        <TextBlock Name="PriceTextBlock" Margin="5,0,0,0"  Width="60" TextAlignment="Right" FontSize="14" FontWeight="Bold" Text="{Binding Path=Ticket.Price,Mode=TwoWay, StringFormat={}{0:##0.00}}"/>
    </StackPanel>
</UserControl>
